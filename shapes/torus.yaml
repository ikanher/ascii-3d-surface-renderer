# Top-level keys (all required):
vars: [u, v]
domain:
  u: [0, 2*pi]
  v: [0, 2*pi]
equations: |
  # Half-twist angle
  half_twist = u / 2

  # Radius of the central circle
  R = 1
  r = 0.3

  # Final coordinates:
  x = (R + r*cos(v)) * cos(u)
  y = (R + r*cos(v)) * sin(u)
  z = r*sin(v)
